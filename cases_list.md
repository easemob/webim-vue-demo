# 功能清单

> SDK 5.0 合并消息下行严格展示 `CombineMessageBody.title`、`summary` 与可选 `messageList`；未下发 `messageList` 时明确提示缺失，不以发送端本地源消息或 `0` 条消息替代。
>
> SDK 5.0 群组黑名单严格按 `GroupBlocklistEntry.user.userId` 展示和判断成员关系，不直接渲染黑名单条目对象。

| 模块            | 具体功能                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 消息-SDK 5.0 当前会话上下文 | SDK 当前会话上下文（进入消息页调用 `setCurrentConversation({ conversationId, conversationType })`，页面展示 `getCurrentConversation()` 真实返回，切换或离开消息页调用 `resetCurrentConversation()`；用于验证当前会话在线消息不累加 SDK 本地未读，不做本地兜底） |
| Demo 约束       | Web 端仅作为服务端能力验证工具<br>界面与控制台真实暴露 SDK 5.0 / 服务端成功与失败结果<br>当前分支零保留 SDK 4.0 代码及兼容代码：不保留旧 API、旧参数、旧事件、旧消息字段转换器、聚合事件适配器、双路径或旧 SDK 发送入口；UI、Store、日志直接使用 SDK 5.0 公开模型<br>每个 SDK 调用的参数名、必填项、类型及下行字段均以当前本地 WebSDK 5.0 类型声明和公开方法签名为唯一依据，调用前必须全字段核对并删除旧 SDK 字段，不保留同义映射或兼容字段<br>失败提示文案与服务端真实错误语义保持一致<br>同一次失败保留单条完整错误日志，避免重复打印干扰排查<br>控制台完整输出 SDK 5.0 调用、响应、事件、失败与重连日志，并用前缀区分 SDK 原始日志、App 事件派发、Store/UI 外层处理日志<br>消息扩展资料处理日志直接输出原始 SDK 5.0 消息及 `msgServerId`、`msgLocalId`、`conversationId`、`conversationType`、`sender.userId`、`timestamp`，不使用旧字段或本地补值<br>SDK 消息解析空引用异常会保留原始错误日志并阻止开发态全屏错误覆盖层，不包装成成功结果<br>SDK 5.0 触发 `onConnecting` 时显示离线提示，收到真实 `onConnected` 后恢复在线显示<br>IM 连接断开、发送超时等真实失败仅打印到控制台，不使用开发态全屏错误覆盖层遮挡页面<br>socketUrl 显式配置 `ws://` 或 `wss://` 时原样使用，未配置协议时默认补 `wss://`<br>消息 `conversationType` 缺失时清晰报错，不本地推断为单聊/群聊/聊天室<br>不提供客户端模拟成功、自动重试、静默降级、失败后自动续跑等兜底行为                              |
| 登录            | 输入环信 ID 与 Token 登录并缓存登录态<br>登录完成后通过 SDK 5.0 公开 `ChatClient.getCurrentUserId()` 读取真实当前用户，再初始化联系人、用户资料、在线状态、群组与会话数据；避免在 `onConnected` 事件内抢跑 REST Manager<br>ChatClient 连接事件监听生命周期（注册 SDK 5.0 `ChatClient.addEventHandler` 前先调用 `removeEventHandler` 清理同 ID 监听）<br>NGI 与线上 VIP6 使用 SDK 5.0 DNS 获取同 AppKey 的真实 REST/WS 地址；TKE/DEV/QA隔舱私有化环境使用用户明确配置的固定服务地址<br>重新登录失败时控制台输出脱敏 AppKey、服务模式/地址、用户 ID、Token 长度及 SDK Provision `statusCode/reason`，不输出 Token 明文                                                                                                                                                                                                                                                   |
| 导航            | 登录后可进入会话、联系人、聊天室三大主导航<br>显示当前运行环境                                                                                                                                                                                                                                                                                                                                                                                                    |
| 会话            | 展示服务端/本地同步的会话列表<br>主动刷新会话列表（调用 SDK 5.0 `refreshSessionList({ includeEmpty: true })`，成功后直接展示 SDK 返回的真实会话列表；失败展示真实错误，不用本地缓存伪造刷新成功）<br>群聊会话详情批量拉取使用 SDK 5.0 `groupManager().getGroup(groupId).getDetail()`；某个群已不存在返回 404/303 时记录该群真实错误，不伪造详情，也不阻塞其他成功群详情展示<br>会话搜索（按 SDK 5.0 最后一条消息的真实内容和时间搜索、展示）<br>查看标星会话<br>会话右键菜单展示置顶、标星、推送通知设置、删除入口<br>会话置顶/取消置顶<br>会话标星/取消标星<br>设置单个会话推送通知方式<br>设置单个会话免打扰时长<br>清除单个会话推送通知方式<br>打开单聊/群聊会话时调用 SDK 5.0 清除未读数；聊天室不支持该 API，因此不调用也不本地伪造清零<br>清空全部会话未读数（调用 SDK 5.0 `clearAllConversationUnreadMessageCount()`，成功后才更新本地未读展示）<br>清空全部消息与会话（调用 SDK 5.0 `clearAllMessagesAndConversations()`，确认后执行，成功后才清空本地会话与消息展示）<br>删除会话                                                                                                                                                                                                                                                         |
| 联系人          | 展示好友列表<br>展示群组列表（SDK 5.0 `GroupManager.getJoinedGroupList()` 只读本地同步快照；登录后立即读取一次，并在 `ChatClient.onSyncDataFinished({ dataType: 'group', status: 'success' })` 后刷新展示；同步失败保留真实 payload/error，不用 REST 兜底或伪造群组）<br>联系人搜索<br>刷新好友列表<br>查看系统通知入口<br>收到好友申请时仅根据 SDK 5.0 `onContactInvited` 展示真实通知；未下发事件时不从联系人快照补造通知<br>联系人事件监听生命周期（注册 SDK 5.0 `ContactManager.addEventHandler` 前先调用 `removeEventHandler` 清理同 ID 监听）<br>好友关系建立后根据 SDK 5.0 `onContactAdded` / `onContactAgreed` 已更新的 `contactManager.getContacts()` 快照刷新列表，不根据旧事件字段猜测好友 ID<br>进入联系人详情页<br>删除好友<br>加入黑名单（仅当 SDK 5.0 返回的 `succeeded` 包含目标用户时显示成功；服务端拒绝或未确认时保持原状态并展示真实错误）<br>从黑名单移除<br>订阅联系人在线状态（仅将 SDK 5.0 联系人快照中真实、非空、非当前登录用户的 `userId` 传给 `subscribePresence`；消息页头部自动订阅在线状态仅限 SDK 5.0 `singleChat` 用户会话；`groupChat` 和 `chatRoom` 不调用 `subscribePresence`；订阅前过滤当前登录用户，避免把群组 ID、聊天室 ID 或自己作为 Presence 订阅目标）<br>取消订阅联系人在线状态<br>刷新联系人在线状态<br>发起单聊会话<br>进入群聊会话                                                                                                                                                                            |
| 用户资料与设置  | 展示当前用户头像与基础信息；当前用户资料查询调用 SDK 5.0 `UserInfoManager.getUserInfoByUserId({ userIds: [userId] })`，SDK 返回空命中时只记录 warning 和真实当前 userId，不抛初始化错误、不伪造昵称/头像/资料<br>编辑用户信息<br>用户资料扩展能力：注册 / 移除用户资料事件监听、按属性查询用户资料、订阅 / 取消订阅陌生人资料变更、查询已订阅用户、按单属性更新当前用户资料；均调用 SDK 5.0 `UserInfoManager` 公开 API 并展示真实结果 / 错误<br>ChatClient 运行诊断：读取 SDK 5.0 `getConnectionState()`、`getRestContext()`、`getCacheManager()`、`getUploadAdapter()`、`getContactSnapshot()` 的真实返回<br>其他平台登录 ID：调用 `ChatClient.getSelfIdsOnOtherPlatform()` 并展示 SDK 原始 `userId/resource` 列表<br>RTC UID 映射：用户输入 RTC UID 数组后调用 `ChatClient.getUserIdsWithRTCUids(rtcUids)` 并展示 SDK 原始映射，未命中不补齐<br>本地语音文件转文字：用户选择浏览器原始 `File` 后调用 `ChatManager.voiceFileToText(file, voiceParams)`，可选填写 `VoiceParams.format`，仅展示 SDK 原始 `{ text }` 或错误<br>Token 续期：用户输入新 Token 后调用 SDK 5.0 `renewToken(token)`；token 明文不写入日志<br>Push 扩展能力：上传 Push Token、设置 / 查询全局免打扰、批量查询会话免打扰、设置 / 查询推送语言、按提醒类型分页查询会话<br>登录后刷新当前用户真实在线状态<br>在线状态事件监听生命周期（注册 SDK 5.0 `PresenceManager.addEventHandler` 前先调用 `removeEventHandler` 清理同 ID 监听）<br>切换在线状态<br>发布自定义在线状态<br>新消息提示音开关<br>SDK 日志开关<br>下载 SDK 缓存日志<br>查看当前运行环境详情<br>查看在线状态订阅列表；订阅列表查询固定从服务端接受的 `pageNum: 1` 开始；Demo 调用前将 `pageNum/pageSize` 规范为正整数；`pageNum: 0` 的 SDK 类型注释与服务端 400 结果不一致，服务端仍拒绝时保留原始错误且不重试、不触发开发态红屏<br>查看黑名单列表<br>退出登录                                                                                                                                                                                                                                               |
| 创建与申请      | 添加好友（SDK 5.0 `contactManager.addContact({ userId, message })`；好友申请与好友建立通知仅以接收端真实 `onContactInvited` / `onContactAdded` 回调展示，未下发时不从联系人列表补造通知）<br>创建群组（严格调用 SDK 5.0 `groupManager.createGroup({ name, description, memberIds, public, joinApprovalRequired, allowInvites, inviteNeedConfirm, maxMembers, ext, avatar })`，可填写群头像与群扩展信息）<br>申请入群                                                                                                                                                                                                                                                                                                                                                                    |
| 消息-基础收发   | 发送文本消息（@ 候选仅按当前 SDK 5.0 会话类型读取：群聊调用 `groupManager.getGroup(groupId).getMembers({ cursor, pageSize })`，聊天室调用 `chatRoomManager.getChatRoom(chatRoomId).getMembers({ cursor, pageSize })`；两者均直接读取返回项 `user.userId`，不得将聊天室 ID 传入群组接口）<br>发送图片消息（本地文件按 SDK 5.0 `createImageMessage` 的 `data`、`filename`、`filetype`、`width`、`height` 参数发送）<br>发送文件消息（本地文件按 SDK 5.0 `createFileMessage` 的 `data`、`filename`、`filetype`、`fileSize` 参数发送）<br>发送视频消息（浏览器从实际文件读取 `duration`、`videoWidth`、`videoHeight` 后，按 SDK 5.0 `createVideoMessage` 的 `data`、`duration`、`width`、`height` 参数发送）<br>发送语音消息（浏览器从实际文件读取 `duration` 后，按 SDK 5.0 `createVoiceMessage` 的 `data`、`duration` 参数发送）<br>发送位置消息<br>发送表情文本消息<br>发送扩展文本消息<br>发送自定义消息（SDK 5.0 `createCustomMessage` 的自定义 JSON 传入 `params`，下行严格从 `body.params` 展示；顶层 `ext` 仅保留消息扩展，不互相补值）<br>发送并展示透传消息<br>发送合并消息<br>分享个人名片消息<br>发送定向消息（组件和请求均只使用 SDK 5.0 `conversationId`、`conversationType`、`receiverList`；群组 `groupChat` 与聊天室 `chatRoom` 均将选择的成员按 SDK 5.0 消息创建参数 `receiverList` 传入；页面只展示 SDK/服务端实际结果，不把未传接收人列表的广播当作定向成功）<br>会话、聊天室、联系人、创建群组和消息话题入口均仅以 `conversationId`、`conversationType` 打开消息页；消息页不读取或兼容旧路由字段<br>SDK 当前会话上下文（进入消息页调用 `setCurrentConversation({ conversationId, conversationType })`，页面展示 `getCurrentConversation()` 真实返回，切换或离开消息页调用 `resetCurrentConversation()`；用于验证当前会话在线消息不累加 SDK 本地未读，不做本地兜底）<br>SDK 5.0 消息回包与历史消息按真实 `conversationId`、`conversationType`、`sender`、`timestamp`、`body` 字段展示；历史查询显式传入 `conversationId`、`conversationType`，不使用旧字段或本地伪消息补齐<br>只投在线用户发送开关（单聊/群聊，含开关切换 toast 和 console 日志）<br>清空当前会话消息展示<br>空消息拦截                                                                                                                                                                              |
| 消息-SDK 5.0 Client 直接发送 | 当前会话文本提供 `SDK5 Client.sendMessage` 独立按钮<br>消息仅以 SDK 5.0 `conversationId`、`conversationType`、`content`、`ext` 和当前发送 options 构造；先由 `ChatManager.createTextMessage` 产生原始 `Message`，再直接调用 `getClient().sendMessage(message, options)`<br>成功只展示 SDK 原始回包；失败完整打印 Console 原始错误，不回退 `ChatManager.sendMessage`、不重试、不本地伪造成功<br>单聊、群聊、聊天室必须分别以真实账号与服务端回包验证，并保留发送端与接收端日志 |
| 消息-原始类型与话题结果 | 运行时展示严格直接判断 SDK 5.0 `Message.type` 的 `text/image/voice/location/file/video/custom/cmd/combine`，不使用 V4 消息类型常量或转换器<br>服务端 `searchMessages({ option: { msgTypes } })` 则直接传 SDK 5.0 独立 `SearchableMessageType` 参数 `txt/img/video/file/loc/custom/combine`，不与运行时 `Message.type` 互相映射<br>消息话题抽屉直接消费 `ChatThreadListResult`、`ChatThreadMemberListResult` 和 `ChatThreadLastMessageListResult` 的原始 `items/cursor`，不封装成自定义 `list` 响应 |
| 消息-展示与交互 | 展示文本、图片、文件、视频、语音、位置、自定义、透传、合并消息<br>图片消息按 SDK 5.0 `ImageMessageBody.thumbnailUrl` 展示缩略图，点击预览 `originalImageUrl` 原图<br>文件消息按 SDK 5.0 `FileMessageBody.fileLength` 展示文件大小<br>消息工具栏图标悬停显示功能名称<br>服务端消息搜索（单聊/群聊/聊天室当前会话或全部可见会话，支持关键词、关键词关系、消息类型、搜索内容范围、可选日期时间范围和分页；未开通服务、关键词个数或长度超限时按服务端真实错误提示并保留 console 日志）<br>文本消息翻译（先读取 SDK 5.0 支持语言列表，再调用 `translateMessage({ message, targetLanguages })`，按真实返回展示 `detectedLanguage` 与 `translations`，失败不兜底）<br>语音消息转文字（对已有语音消息调用 SDK 5.0 `voiceMessageToText(message.body)`，按真实 `text` 展示，失败不兜底）<br>SDK 附件下载（图片 / 视频 / 文件 / 语音消息右键调用 `downloadAttachment({ message })`，展示 SDK 返回文件名、类型、大小、下载地址和二进制长度；失败不使用直链兜底）<br>置顶消息列表（消息页头部调用 `getPinnedMessageList({ conversationId, conversationType })`，按 SDK 返回 `items` 展示）<br>消息事件监听生命周期（登录初始化注册消息、撤回、编辑、回执、Reaction、多设备等 SDK 5.0 Chat 事件前，先调用 `ChatManager.removeEventHandler(handlerId)` 清理同 ID 监听，再调用 `addEventHandler(handlerId, handlers)` 注册；不使用旧事件聚合或本地兼容映射）<br>图片粘贴预览发送<br>消息引用（发送文本时将被引用的 SDK 5.0 原始 Message 直接置于 `ext.quote`；展示和定位仅读取 `msgServerId`、`msgLocalId`、`sender`、`type`、`body`，不创建旧引用消息模型）<br>消息复制<br>消息撤回；调用 SDK 5.0 `recallMessage({ conversationId, conversationType, messageId })`，成功后本地标记消息为撤回；若随后刷新会话列表时 SDK cache 暂无会话快照，只记录非阻塞 warning，不展示为撤回失败<br>文本消息编辑<br>群成员退出事件严格展示 SDK 5.0 `onMembersExited.groupId` 与 `onMembersExited.members`；该事件未提供操作者时不伪造“谁移除成员”<br>跨端编辑和撤回仅按 SDK 5.0 事件下发的 `messageId`、`conversationId`、`conversationType` 定位；定位字段未下发时保留真实事件和错误，不使用旧模型字段补齐<br>删除消息<br>消息举报当前 SDK 5.0 未公开 API，Demo 不提供入口，详见 `docs/sdk5-unsupported-capabilities.md`<br>消息置顶<br>取消消息置顶<br>单聊右侧资料面板设置好友备注、消息免打扰、加入/移出黑名单、查看黑名单列表并移出用户、清空聊天记录、删除联系人；其中备注、删除联系人、加入/移出黑名单只使用 SDK 5.0 明确解析出的用户 ID，`conversationId` 仅作为会话 ID 展示和用于会话免打扰/清消息，未解析到用户 ID 时展示真实原因并停止调用联系人 API<br>群聊消息右键创建消息话题<br>群组子区列表查看并进入消息话题会话<br>消息话题会话内返回父群组<br>消息话题会话内发送、接收、撤回和获取历史消息<br>批量获取消息话题最新消息<br>加入、退出、改名、解散消息话题<br>查看消息话题详情、refresh 详情、成员列表并移出成员<br>查看我加入的消息话题列表<br>监听消息话题事件                                                                                                                           |
| 消息-状态       | 单聊消息送达回执状态展示<br>单聊消息已读回执状态展示<br>群聊文本消息发送时开启群消息已读回执统计<br>群消息已读人数展示<br>群消息已读用户列表（群消息右键调用 SDK 5.0 `getGroupMessageReadUsers({ groupId, messageId, cursor, pageSize })`）<br>群消息回执详情（群消息右键调用 SDK 5.0 `getGroupMessageReadReceipts({ groupId, messageIds })`）                                                                                                                                                                                                                                                                                                                                                                            |
| 群组            | 展示 SDK 5.0 原始 `GroupDetail` 的群名称、群描述、群头像、群扩展、群公告、成员数量和人数上限；字段未下发时明确展示缺失值，不使用旧字段或本地默认人数<br>读取群轻量摘要；调用 SDK 5.0 `groupManager.getGroup(groupId).getSummary()` 并展示 SDK 返回的真实 `JoinedGroupSummary | null`<br>修改群名称、描述、头像和扩展信息；先调用 `groupManager.getGroup(groupId).updateInfo(...)`，修改群头像时为 `groupManager.getGroup(groupId).updateInfo({ avatar })`，后调用 `refresh()`，只展示返回的原始 `GroupDetail`<br>群配置与群主：调用 SDK 5.0 `groupManager.getGroup(groupId).updateConfigs({ public, joinApprovalRequired, allowInvites, inviteNeedConfirm, maxMembers })` 更新群配置；调用 `changeOwner({ newOwner })` 转让群主；成功后 `refresh()` 并展示 SDK 真实群详情，失败展示真实错误<br>查看群公告<br>修改群公告<br>查看群共享文件列表<br>上传群共享文件<br>下载群共享文件<br>删除群共享文件<br>查看群成员列表与定向消息默认接收人；调用 SDK 5.0 `groupManager.getGroup(groupId).getMembers({ cursor, pageSize })`，按返回项 `user.userId`、`role`、`joinedAt` 展示/填充，资料未下发昵称时展示真实 userId<br>查看群管理员列表；调用 SDK 5.0 `groupManager.getGroup(groupId).getAdmins()`，直接展示返回的 `GroupUserInfo[]` 的 `admin.userId`<br>添加群管理员；调用 `addAdmin({ userId })`，成功后重新查询管理员和成员列表<br>移除群管理员；调用 `removeAdmin({ userId })`，成功后重新查询管理员和成员列表<br>群成员属性 / 群名片（调用 SDK 5.0 `groupManager.getGroup(groupId).setMemberAttributes({ userId, memberAttributes: { groupNamecard } })` 设置，调用 `getMembersAttributes({ userIds, keys: ['groupNamecard'] })` 查询并展示真实 `items` 和原始属性 JSON；失败保留 SDK / 服务端真实错误）<br>查看群白名单；调用 SDK 5.0 `groupManager.getGroup(groupId).getAllowlist()` 并按 `entry.user.userId` 展示<br>添加群成员到白名单；调用 `addUsersToAllowlist({ userIds })`，成功后重新查询白名单<br>将群成员移出白名单；调用 `removeUsersFromAllowlist({ userIds })`，成功后重新查询白名单<br>查询当前用户是否在群白名单；调用 `checkIfInAllowList()` 并展示 SDK 真实 boolean<br>邀请好友入群；邀请列表只展示当前未在群内的好友，权限判断使用 SDK 5.0 `allowInvites` 字段，邀请失败时展示 SDK / 服务端真实错误<br>群组系统通知严格监听 SDK 5.0 的命名群事件；邀请、申请、成员进退、管理员/群主、禁言/白名单、公告、共享文件、群信息和解散均按 SDK 5.0 回调的 `groupId` 与用户资料字段展示，不读取 SDK 4.0 聚合事件或旧字段；未下发操作者时不显示 undefined<br>群组事件监听生命周期（注册 SDK 5.0 `GroupManager.addEventHandler` 前先调用 `removeEventHandler` 清理同 ID 监听）<br>移出群成员<br>开启/关闭群组消息免打扰<br>查看群黑名单<br>添加群成员到黑名单<br>将群成员移出黑名单<br>查看群禁言名单；禁言名单按 SDK 5.0 `user.userId`、`muteExpire` 展示<br>禁言群成员<br>解除群成员禁言<br>群全员禁言；调用 SDK 5.0 `groupManager.getGroup(groupId).muteAllMembers()`，成功后 `refresh()` 并查询当前用户禁言状态<br>解除群全员禁言；调用 SDK 5.0 `groupManager.getGroup(groupId).unmuteAllMembers()`，成功后 `refresh()` 并查询当前用户禁言状态<br>查询当前用户群禁言状态；调用 SDK 5.0 `groupManager.getGroup(groupId).checkIfInMuteList()` 并展示真实 boolean<br>退出群组<br>解散群组 |
| 群组-SDK 事件 | 全部 26 个 SDK 5.0 `GroupEventName` 命名事件均注册监听；真实触发时 console 输出事件名、原始 payload、payload.groupId 与当前用户；不使用 V4 聚合事件或本地模拟事件 |
| Reaction        | 单聊消息添加 Reaction<br>单聊消息取消 Reaction<br>单聊消息查看 Reaction 详情<br>群聊消息添加 Reaction<br>群聊消息取消 Reaction<br>群聊消息查看 Reaction 详情                                                                                                                                                                                                                                                                                  |
| 聊天室-列表     | 展示 SDK 5.0 返回的所有聊天室列表<br>刷新聊天室列表；刷新聊天室列表失败时保留 SDK / 服务端真实错误；若 SDK 5.0 返回 token 失效、未登录或未授权则清除过期登录态并回到登录页，不重试、不伪造列表<br>已加入聊天室列表当前 SDK 5.0 未提供公开 API，详见 `docs/sdk5-unsupported-capabilities.md`                                                                                                                                                                                                                                                                                         |
| 聊天室-列表展示 | 列表直接展示 SDK 5.0 `ChatRoomSummary.chatRoomId`，不展示固定“未返回描述”文案，也不为描述额外请求详情 |
| 聊天室-成员关系 | 加入聊天室<br>同一运行会话中，只有 `joinChatRoom` 返回成功的指定房间显示“进入聊天室”；聊天室创建者或管理员的 `ChatRoomDetail.permissionType` 不作为已加入成员关系；该确认状态由应用级内存保存，切换主导航后仍有效，退出登录时清空；该按钮只进入消息页，不再次发起加入请求；刷新公开列表不会推测其他房间成员关系<br>加入请求进行中阻止同一聊天室重复提交，成功或失败仍以真实 SDK / 服务端返回为准；权限拒绝（`210`）以及 SDK 映射为 `202` 但 `details.reason` 明确为黑名单的业务拒绝仅展示原始失败，不清空登录态或跳转登录页<br>退出聊天室成功后恢复“加入”<br>从聊天室详情进入消息页                                                                                                                                                                                                                                                                                                          |
| 聊天室-详情     | 展示聊天室基础信息<br>重复进入详情时重新请求服务端详情<br>刷新聊天室详情；调用 SDK 5.0 `chatRoomManager.getChatRoom(chatRoomId).refresh()` 并展示 SDK 返回的真实 `ChatRoomDetail`<br>展示聊天室公告<br>聊天室消息免打扰当前 SDK 5.0 不支持，详情见 `docs/sdk5-unsupported-capabilities.md`<br>修改聊天室信息<br>更新聊天室公告<br>获取聊天室自定义属性<br>添加单个自定义属性<br>删除单个自定义属性<br>批量添加自定义属性<br>批量删除自定义属性                                                                                                                                                                                                                  |
| 聊天室-消息     | 发送文本消息<br>发送图片消息<br>发送视频消息<br>发送文件消息<br>发送语音消息<br>发送位置消息<br>发送自定义消息<br>发送并展示透传消息<br>发送扩展文本消息<br>聊天室合并消息当前 SDK 5.0 下行退化为兼容文本，详情见 `docs/sdk5-unsupported-capabilities.md`<br>发送定向文本消息<br>分享个人名片<br>文本消息复制<br>文本消息翻译<br>语音消息转文字<br>文本消息引用<br>文本消息撤回<br>文本消息编辑<br>删除消息                                                                                                                                                            |
| 聊天室-成员管理 | 进入聊天室成员管理页<br>查看成员列表；按 SDK 5.0 `chatRoomManager.getChatRoom(chatRoomId).getMembers({ cursor, pageSize })` 与返回项 `user.userId` 展示，不读取旧成员字段或伪造成员<br>查看黑名单<br>查看白名单<br>查看禁言列表<br>查询当前用户聊天室禁言状态；调用 SDK 5.0 `chatRoomManager.getChatRoom(chatRoomId).checkIfInMuteList()` 并展示真实 `ChatRoomMuteStatus.muted` 与 `muteExpireAt`；失败展示真实错误，不显示为“未禁言”<br>查看管理员列表<br>添加到黑名单<br>移出黑名单<br>添加到白名单<br>移出白名单<br>禁言指定成员<br>解除指定成员禁言<br>全员禁言<br>取消全员禁言<br>添加管理员<br>移除管理员<br>移出聊天室成员                                                                                                                                                                  |
| 聊天室-原始成员模型 | 聊天室路由、日志和 Store 状态仅使用 `chatRoomId`<br>成员列表直接保存 `ChatRoomMemberEntry` 并从 `entry.user.userId` 展示/操作，不创建顶层 `userId` 映射<br>黑名单、白名单和禁言名单直接保存 SDK 5.0 `ChatRoomBlocklistEntry`、`ChatRoomAllowlistEntry`、`ChatRoomMuteEntry`，从 `entry.user.userId` 与 `entry.muteExpire` 展示/操作；管理员为原始 `UserInfo[]`，从 `admin.userId` 展示 |
| 聊天室-SDK 事件 | 登录后全局注册并直接展示 SDK 5.0 命名聊天室事件；聊天室全部 16 个 SDK 5.0 命名事件触发时均在 console 输出 handlerId、事件名、原始 rawEvent、payload.chatRoomId 与当前用户；详情和成员管理页仅按 payload 原始 `chatRoomId` 刷新成员、管理员、禁言名单、白名单、公告和自定义属性，不重复打印事件日志，不使用 V4 聚合事件或字段转换 |
